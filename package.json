{
  "name": "zybra-rosca-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for Zybra ROSCA Finance - Tracking yield, contributions, and member investments",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ zybra-money",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zybra-rosca",
    "create:local": "graph create --node http://localhost:8020/ zybra-rosca",
    "remove:local": "graph remove --node http://localhost:8020/ zybra-rosca",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
